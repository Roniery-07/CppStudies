#include <iostream>
#include <string>
#include <array>
#include <vector>
#include <sstream>
int main()
{

    // std::array<int, 10> nameC = {1, 2, 3, 4, 5};
    // std::string nameS = "asdfasdf";
    // std::vector<int> v = {1, 2, 3, 4, 5};

    // std::cout << "String: " << sizeof(nameS) << "\n"
    //           << "string.size(): " << nameS.size() << "\n"
    //           << "sizeof(array): " << sizeof(nameC) << "\n"
    //           << "Letra: " << sizeof("l") << "\n"
    //           << "Length: " << nameS.length() << "\n";

    // std::string linha;

    // getline(std::cin, linha);

    // std::istringstream flow(linha);

    // std::string valor;
    // while (getline(flow, valor, ','))
    // {
    //     std::cout << "[" << valor << "]" << std::endl;
    // }

    // std::string nome = "Fulano ";
    // std::string sobrenome = "Silva";

    // std::cout << nome + sobrenome << std::endl;
    // std::cout << nome << sobrenome << std::endl;
    // std::cout << nome.append(sobrenome) << std::endl;

    // std::cout << "\n"
    //           << nome << std::endl;

    int a, b;

    std::cin >> a >> b;

    std::cout << "a: " << a << std::endl;
    std::cout << "b: " << b << std::endl;

    return 0;
}